const LocationInfo = ( {data} ) => {
      return(
            <>
            {/* {data &&
                 data.map( (dat)=>{
                       return(
                             <div className="card-info-planet">
                             <span>{ data.name } </span>
                             <span>{ data.type } </span>
                             <span> residents : { data.residents } </span>
                             </div>
                       )
                 } )
            } */}
            </>
      )
}
export default LocationInfo;